# issue
### Navigation issue while using BrowserRouter
    - While running the application in tizen simulator the pages where rendering but not navigationg because of the use of BrowserRouter. Changed it to HashRouter and now it works on simulator.
 ### Horizontal scroll bar appear
    -As width was set to 100vw a horizontal scroll bar appeared and when changed to 100% it is gone.
